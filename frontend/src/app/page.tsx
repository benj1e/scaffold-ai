import React from "react";
import Hero from "./elements/hero";

const Page = () => {
    return (
        <div className="flex flex-col">
            <Hero />
        </div>
    );
};

export default Page;
